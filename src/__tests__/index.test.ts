import { index } from '../index'

test('generate between 0 and 1', () => {
    expect(index(0,1)).toEqual(1);
});

test('generate between 0 and 1', () => {
    expect(index(0,1)).toEqual(1);
});

test('generate between 0 and 1', () => {
    expect(index(0,1)).toEqual(1);
});

test('generate between 0 and 1', () => {
    expect(index(0,1)).toEqual(1);
});
